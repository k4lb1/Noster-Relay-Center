# Optional: Default relay URL pre-filled in the connection form.
# Leave empty for public deployment (each user enters their own relay).
# Example for a branded instance: wss://0x-nostr-relay.fly.dev
VITE_DEFAULT_RELAY_URL=
